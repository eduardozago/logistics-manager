services:
  postgres:
    container_name: logistics-postgres
    image: postgres
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: logistics-manager
      PGDATA: /data/postgres
    volumes: 
      - ./data/pg:/data/postgres:z